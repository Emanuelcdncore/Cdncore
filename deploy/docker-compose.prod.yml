services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "127.0.0.1:5432:5432"
    environment:
      POSTGRES_DB: cdnlandings
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database:/database:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped
    mem_limit: 512M

  migrate:
    image: postgres:16-alpine
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/cdnlandings
    volumes:
      - ./database:/database:ro
    entrypoint: ["/bin/sh", "-c", "cd / && /database/migrate.sh"]
    restart: "no"
    profiles:
      - migration

  cdncore:
    build:
      context: .
      dockerfile: apps/cdncore/Dockerfile
      args:
        BASE_PATH: /cdncore
    expose:
      - "3000"
    ports:
      - "127.0.0.1:3001:3000"
    restart: unless-stopped
    mem_limit: 512M

  cdntek:
    build:
      context: .
      dockerfile: apps/cdntek/Dockerfile
      args:
        BASE_PATH: /cdntek
    expose:
      - "3000"
    ports:
      - "127.0.0.1:3002:3000"
    restart: unless-stopped
    mem_limit: 512M

  cdntv:
    build:
      context: .
      dockerfile: apps/cdntv/Dockerfile
      args:
        BASE_PATH: /cdntv
    expose:
      - "3000"
    ports:
      - "127.0.0.1:3003:3000"
    restart: unless-stopped
    mem_limit: 512M

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - cdncore
      - cdntek
      - cdntv
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1/health"]
      interval: 10s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
      - NET_BIND_SERVICE
    mem_limit: 128M

volumes:
  postgres_data:
